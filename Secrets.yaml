# Your Wi-Fi SSID and password
wifi_ssid: "{{WiFi SSID}}"
wifi_password: "{{password}}"

# Web Server authentication
web_server_username: "{{ESPHome device username}}"
web_server_password: "{{ESPHome device}}"

# AP password
ap_password: "{{fallback accesspoint password}}"

# API encryption key
# Requirement: base64, 32bit
api_encryption_key: "{{API encryption key}}"

# OTA password
ota_password: "{{OTA Password}}"
